- name: Install nginx
  yum:
    name: nginx
    state: latest

- name: start nginx
  ansible.builtin.systemd:
    state: restarted
    name: nginx
    enabled: yes

- name: download code
  include_role:
    name: common
    tasks_from: download

- name: remove lod content
  ansible.builtin.file:
    path: /usr/share/nginx/html
    state: absent

- name: copy the Htdocs content
  ansible.builtin.copy:
    src: /tmp/frontend-main/static/
    dest: /usr/share/nginx/html/
    remote_src: yes

